.Content
  .Content__wrapper
    .Title
      %h2 Account Information
    .ShowAccount
      = render partial: 'user_info', locals: { user: @user }
  .Content__bottom
    %p.Content__questions= "あなたの質問はこちら"
    %ul.User-Questions_hidden
      - @user_questions.each do |question|
        %li.User-Questions__item
          = link_to "#{question.title}", user_path(question.id), class: "User_Questions__link"
    %p.Content__answers= "あなたの回答はこちら"
    %ul.User-Answers_hidden
      - @user_answers.each do |answer|
        %li.User-Answers__item
          = link_to "#{answer.question.title}", user_path(answer.question.id), class: "User_Answers__link"