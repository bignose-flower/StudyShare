.Content{style: 'background-color: #cdc2b8;'}
  .QuestionFormBox
    .QuestionForm
      .QuestionTitle
        = @question.title
      .QuestionText
        = @question.question
      .QuestionCategory
        .ChoiceBox
          = @question.category
      = link_to "メイン画面に戻る", root_path